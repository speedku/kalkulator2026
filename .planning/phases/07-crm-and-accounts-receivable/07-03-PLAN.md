---
phase: 07-crm-and-accounts-receivable
plan: "03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/app/(dashboard)/windykacja/page.tsx
  - src/app/(dashboard)/windykacja/[caseId]/page.tsx
  - src/app/(dashboard)/windykacja/_components/aging-chart.tsx
  - src/app/(dashboard)/windykacja/_components/overdue-table.tsx
  - src/app/(dashboard)/windykacja/_components/send-reminder-dialog.tsx
  - src/app/(dashboard)/windykacja/_components/case-status-badge.tsx
  - src/app/api/windykacja/pdf/[invoiceId]/route.ts
  - src/lib/pdf/windykacja-template.tsx
  - src/app/(b2b)/layout.tsx
  - src/app/(b2b)/portal/price-list/page.tsx
autonomous: true
requirements: [WIND-01, WIND-02, WIND-03, WIND-04, CRM-05]

must_haves:
  truths:
    - "Admin sees aging dashboard with bucket summary bar chart and overdue invoices table"
    - "Admin can open a case for an overdue invoice and track its status"
    - "Admin can send a payment reminder email from the case detail page"
    - "Sent reminders are logged in ReminderLog and visible in case history"
    - "Admin can generate a windykacja PDF document (collection notice) for an invoice"
    - "B2B portal at /portal/price-list shows the logged-in user's assigned price list"
    - "B2B portal is gated by requireAuth() — unauthenticated users redirect to /login"
    - "npx next build exits 0 with all windykacja and b2b routes"
  artifacts:
    - path: "src/app/(dashboard)/windykacja/page.tsx"
      provides: "Aging dashboard with bucket summary + overdue table"
      contains: "requireAdmin"
    - path: "src/app/(dashboard)/windykacja/_components/aging-chart.tsx"
      provides: "Recharts BarChart with 4 aging buckets"
    - path: "src/app/(dashboard)/windykacja/_components/send-reminder-dialog.tsx"
      provides: "Dialog for sending payment reminder email"
    - path: "src/app/api/windykacja/pdf/[invoiceId]/route.ts"
      provides: "PDF generation Route Handler"
      contains: "renderToBuffer"
    - path: "src/lib/pdf/windykacja-template.tsx"
      provides: "react-pdf Document template for collection notice"
    - path: "src/app/(b2b)/layout.tsx"
      provides: "B2B portal layout with requireAuth() guard"
      contains: "requireAuth"
    - path: "src/app/(b2b)/portal/price-list/page.tsx"
      provides: "Customer B2B price list view"
  key_links:
    - from: "src/app/(dashboard)/windykacja/page.tsx"
      to: "src/lib/dal/windykacja.ts"
      via: "getAgingData() server call"
      pattern: "getAgingData"
    - from: "src/app/(dashboard)/windykacja/_components/send-reminder-dialog.tsx"
      to: "src/lib/actions/windykacja.ts"
      via: "sendReminderAction via useFormState"
      pattern: "sendReminderAction"
    - from: "src/app/api/windykacja/pdf/[invoiceId]/route.ts"
      to: "src/lib/pdf/windykacja-template.tsx"
      via: "renderToBuffer(<WindykacjaPdfTemplate />)"
      pattern: "renderToBuffer"
    - from: "src/app/(b2b)/portal/price-list/page.tsx"
      to: "src/lib/dal/price-lists.ts"
      via: "getPriceListById(user.priceListId)"
      pattern: "getPriceList"
---

<objective>
Build the windykacja (accounts receivable) UI — aging dashboard, case detail, reminder send, PDF generation — and the B2B portal. This completes Phase 7 by covering WIND-01 through WIND-04 and CRM-05.

Purpose: Delivers the AR collection workflow and the customer-facing B2B portal. Depends on 07-01 DAL (aging, case, reminder functions) and 07-02 for layout pattern consistency.
Output: 10 new files. All windykacja and B2B routes build cleanly.
</objective>

<execution_context>
@C:/Users/mgwaj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mgwaj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crm-and-accounts-receivable/07-RESEARCH.md
@.planning/phases/07-crm-and-accounts-receivable/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Windykacja pages (aging dashboard + case detail) + PDF Route Handler + PDF template</name>
  <files>
    src/app/(dashboard)/windykacja/page.tsx
    src/app/(dashboard)/windykacja/[caseId]/page.tsx
    src/app/(dashboard)/windykacja/_components/aging-chart.tsx
    src/app/(dashboard)/windykacja/_components/overdue-table.tsx
    src/app/(dashboard)/windykacja/_components/send-reminder-dialog.tsx
    src/app/(dashboard)/windykacja/_components/case-status-badge.tsx
    src/app/api/windykacja/pdf/[invoiceId]/route.ts
    src/lib/pdf/windykacja-template.tsx
  </files>
  <action>
**src/app/(dashboard)/windykacja/page.tsx** — Server Component. requireAdmin(). Call getAgingData(). Render:
- PageHeader "Windykacja — Przeterminowane płatności"
- Summary row: 4 GlassCards (each with px-6 py-6) showing bucket totals — "0–30 dni", "31–60 dni", "61–90 dni", "90+ dni" — count of invoices + total PLN per bucket. Show total overdue amount and count in a prominent StatCard at top.
- AgingChart component (receives summary.buckets as prop)
- OverdueTable component (receives rows array as prop)

**src/app/(dashboard)/windykacja/_components/aging-chart.tsx** — "use client". Recharts BarChart. Receives buckets: Record<AgingBucket, { count: number; amount: number }> prop. Four bars (one per bucket, excluding "current"). X-axis: bucket labels. Y-axis: amount in PLN. Tooltip shows count + amount. Bar fill colors: 0-30=amber-400, 31-60=orange-400, 61-90=red-400, 90+=red-700. Wrap in ResponsiveContainer width="100%" height={300}. Import from "recharts".

**src/app/(dashboard)/windykacja/_components/overdue-table.tsx** — "use client". Receives rows: AgingRow[]. TanStack Table with columns: Invoice # (link to /windykacja/[caseId] if caseId exists, else "Otwórz sprawę" button calling createWindykacjaCaseAction(invoiceId)), Customer name, NIP, Total gross (PLN formatted), Due date (date-fns "dd.MM.yyyy"), Days overdue, Bucket badge (color-coded: 0-30=amber, 31-60=orange, 61-90=red, 90+=dark red), Case status badge (if caseId), Actions (PDF link, Send reminder button). Sort by daysOverdue desc by default. "Otwórz sprawę" button calls createWindykacjaCaseAction as a direct server action — import from "@/lib/actions/windykacja". After action, router.refresh().

**src/app/(dashboard)/windykacja/_components/case-status-badge.tsx** — "use client" small component. Receives status: string. Returns a colored badge span: open=sky, reminded=amber, in_dispute=orange, settled=emerald, written_off=zinc.

**src/app/(dashboard)/windykacja/_components/send-reminder-dialog.tsx** — "use client". State-toggled modal (useState showDialog). Receives caseId: number, currentLevel: number (last reminder level + 1, max 4), invoiceNumber: string, recipientEmail: string. Form with: level display (read-only, shows next level), recipientEmail input (pre-filled, editable), submit button "Wyślij przypomnienie". useFormState with sendReminderAction. Hidden inputs for caseId and level. On success (ActionState.success), close dialog and show sonner toast.success. On error, show toast.error.

**src/app/(dashboard)/windykacja/[caseId]/page.tsx** — Server Component. requireAdmin(). params.caseId → getWindykacjaCase(Number(caseId)). If null, notFound(). Render:
- PageHeader: "Sprawa: " + invoice number. Back link to /windykacja.
- GlassCard 1 (px-6 py-6): Invoice details — invoice number, customer name, NIP, total gross, due date, days overdue.
- GlassCard 2 (px-6 py-6): Case management — current status (CaseStatusBadge), priority badge, assigned to, notes. Status update select + button: calls updateCaseStatusAction. "Pobierz PDF wezwania" link to /api/windykacja/pdf/[invoiceId].
- GlassCard 3 (px-6 py-6): Reminder history — table of reminderLogs (level, sent at, recipient, status badge). SendReminderDialog component with currentLevel = last level + 1 (or 1 if no logs), invoiceNumber, recipientEmail from invoice customerEmail or empty.

**src/lib/pdf/windykacja-template.tsx** — react-pdf Document template. No @ts-nocheck needed (Phase 04-03 decision). Import Document, Page, Text, View, StyleSheet from "@react-pdf/renderer". Props: invoice { invoiceNumber: string; customerName: string; customerNip?: string | null; totalGross: number; dueAt: Date | null; daysOverdue: number }. Content: ALLBAG header (company name, address), date of document generation, section "WEZWANIE DO ZAPŁATY", customer block (name, NIP), invoice details (number, amount, due date), overdue message ("Uprzejmie informujemy, że wyżej wymieniona faktura jest przeterminowana o X dni."), payment instruction, signature block. StyleSheet: fontFamily "Helvetica" (built-in, no font registration needed). Export as WindykacjaPdfTemplate.

**src/app/api/windykacja/pdf/[invoiceId]/route.ts** — Route Handler. Named export: export async function GET(request: Request, { params }: { params: Promise<{ invoiceId: string }> }). IMPORTANT: Next.js 15 dynamic route params are async (Promises) — await params first. requireAdmin(). getInvoiceById(Number(invoiceId)) from invoices DAL. If null, return new Response("Not found", { status: 404 }). renderToBuffer from "@react-pdf/renderer". Pass invoice data and compute daysOverdue via differenceInDays. Buffer.from(buffer) wrapping (Phase 04-02 pattern — renderToBuffer returns Buffer<ArrayBufferLike> not assignable to BodyInit directly). Return Response with Content-Type: "application/pdf", Content-Disposition: attachment; filename="wezwanie-{invoiceNumber}.pdf".

CRITICAL import: `import { renderToBuffer } from "@react-pdf/renderer"` — not in a Server Action, only in Route Handler (Node.js context only, not Edge runtime).
  </action>
  <verify>
    <automated>cd C:/xampp/htdocs/kalkulator2026 && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm /windykacja and /windykacja/[caseId] pages compile. Confirm /api/windykacja/pdf/[invoiceId]/route.ts uses Buffer.from() wrapping and async params.</manual>
  </verify>
  <done>npx tsc --noEmit exits 0. 8 files created. WindykacjaPdfTemplate has no @ts-nocheck. Route Handler awaits params (Next.js 15 pattern). Buffer.from() wrapping applied to renderToBuffer output.</done>
</task>

<task type="auto">
  <name>Task 2: B2B portal (route group layout + price list page)</name>
  <files>
    src/app/(b2b)/layout.tsx
    src/app/(b2b)/portal/price-list/page.tsx
  </files>
  <action>
**src/app/(b2b)/layout.tsx** — Server Component (no "use client"). This is the root layout for the (b2b) route group — it MUST export a default function with children: React.ReactNode prop. CRITICAL: Call requireAuth() (not requireAdmin() — B2B users are regular users with priceListId, per RESEARCH.md Pattern 5). If no session, requireAuth redirects to /login automatically.

Layout structure (minimal, separate from (dashboard) AetherShell):
```
<html lang="pl">
  <body className="min-h-screen bg-gray-950 text-white">
    <header className="border-b border-white/10 px-6 py-4 flex items-center justify-between">
      <div className="flex items-center gap-3">
        <span className="text-lg font-bold tracking-wide">ALLBAG B2B Portal</span>
      </div>
      <span className="text-sm text-gray-400">{session.user?.name ?? session.user?.email}</span>
    </header>
    <main className="max-w-5xl mx-auto px-6 py-8">
      {children}
    </main>
  </body>
</html>
```
Import session from requireAuth() call result. No sidebar, no topbar, no Framer Motion — minimal portal shell.

**src/app/(b2b)/portal/price-list/page.tsx** — Server Component. Call requireAuth() to get session (user). Check session.user.priceListId — if null or undefined, render a message "Nie masz przypisanego cennika. Skontaktuj się z administratorem." instead of the table. If priceListId exists, call getPriceListById(session.user.priceListId) or getPriceLists() and filter. Display:
- Page title "Twój cennik" with the price list name as subtitle
- Table with columns: Product name, Product group, Unit, Purchase price (hidden — do not show purchase price in B2B portal), Sale price (computed from margin), margin is NOT shown. Only show: product name, group, calculated sale price formatted as "X XXX,XX PLN".
- If no products in price list, show "Cennik jest pusty."

For price list data: use existing getPriceLists() DAL or create a B2B-specific query. The PriceList has PriceListMargin entries linking to product groups. Use getProductsForBuilder() (established in Phase 04-02) to get products, then apply margin from the price list margin matrix to compute sale prices. Use the existing calculateSalePrice function from src/lib/pricing.ts (or equivalent established in Phase 03).

Import note: DAL functions are server-only. Page is a Server Component — direct DAL imports are fine here.

RESEARCH.md architectural decision: Gate B2B by User.priceListId IS NOT NULL. Same Auth.js v5 session as the dashboard. No separate auth system.
  </action>
  <verify>
    <automated>cd C:/xampp/htdocs/kalkulator2026 && npx next build 2>&1 | grep -E "(error|Error|Route|✓|/windykacja|/portal|/b2b)" | head -30</automated>
    <manual>Confirm /portal/price-list route appears in build output. Confirm (b2b)/layout.tsx uses requireAuth not requireAdmin.</manual>
  </verify>
  <done>npx next build exits 0. Build output shows /windykacja, /windykacja/[caseId], /portal/price-list, /api/windykacja/pdf/[invoiceId] routes. (b2b)/layout.tsx calls requireAuth(). Price list page hides purchase prices and shows only computed sale prices.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits 0
2. `npx next build` exits 0
3. Build output includes: /windykacja, /windykacja/[caseId], /portal/price-list, /api/windykacja/pdf/[invoiceId]
4. WindykacjaPdfTemplate has no @ts-nocheck (Phase 04-03 decision)
5. PDF Route Handler uses Buffer.from(buffer) wrapping (Phase 04-02 pattern)
6. PDF Route Handler awaits params (Next.js 15 async params pattern)
7. B2B layout calls requireAuth() (not requireAdmin())
8. B2B price list page hides purchase prices
9. AgingChart uses Recharts ResponsiveContainer with bucket color coding
</verification>

<success_criteria>
- Aging dashboard shows bucket chart + overdue invoices table with case open/link functionality
- Case detail page shows invoice data + reminder history + SendReminderDialog
- PDF Route Handler generates collection notice PDF with correct Content-Disposition header
- WindykacjaPdfTemplate renders ALLBAG header, customer block, invoice details, overdue message
- B2B portal layout is separate from AetherShell (no sidebar); gated by requireAuth()
- B2B price list page shows computed sale prices; hides purchase prices and margins
- All 10 files TypeScript-clean; full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-crm-and-accounts-receivable/07-03-SUMMARY.md`
</output>
