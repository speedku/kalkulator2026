---
phase: 07-crm-and-accounts-receivable
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(dashboard)/crm/page.tsx
  - src/app/(dashboard)/crm/new/page.tsx
  - src/app/(dashboard)/crm/[id]/page.tsx
  - src/app/(dashboard)/crm/_components/customer-form.tsx
  - src/app/(dashboard)/crm/_components/customer-table.tsx
  - src/app/(dashboard)/crm/leads/page.tsx
  - src/app/(dashboard)/crm/leads/_components/lead-form.tsx
  - src/app/(dashboard)/crm/leads/_components/lead-table.tsx
  - src/app/(dashboard)/crm/pipeline/page.tsx
  - src/app/(dashboard)/crm/pipeline/_components/pipeline-board.tsx
  - src/app/(dashboard)/crm/brand-protection/page.tsx
  - src/app/(dashboard)/crm/brand-protection/_components/brand-watch-table.tsx
autonomous: true
requirements: [CRM-01, CRM-02, CRM-03, CRM-04, CRM-06]

must_haves:
  truths:
    - "Admin can see customer list with search and active/inactive toggle"
    - "Admin can create and edit customers via a form"
    - "Admin can deactivate (soft delete) a customer"
    - "Admin can see customer detail page with leads, deals, and case summary"
    - "Admin can view leads list with status filter and create/edit leads"
    - "Admin can see pipeline board with deals grouped by stage"
    - "Admin can view brand protection watchlist and add/edit/delete items"
    - "npx next build exits 0 with all CRM routes"
  artifacts:
    - path: "src/app/(dashboard)/crm/page.tsx"
      provides: "Customer list page"
      contains: "requireAdmin"
    - path: "src/app/(dashboard)/crm/_components/customer-table.tsx"
      provides: "TanStack Table customer list with search + isActive filter"
    - path: "src/app/(dashboard)/crm/_components/customer-form.tsx"
      provides: "Create/edit customer form with react-hook-form + zodResolver"
    - path: "src/app/(dashboard)/crm/leads/page.tsx"
      provides: "Lead list page"
    - path: "src/app/(dashboard)/crm/pipeline/page.tsx"
      provides: "Deal pipeline grouped by stage"
    - path: "src/app/(dashboard)/crm/brand-protection/page.tsx"
      provides: "Brand watchlist CRUD"
  key_links:
    - from: "src/app/(dashboard)/crm/page.tsx"
      to: "src/lib/dal/crm.ts"
      via: "getCustomers() server call"
      pattern: "getCustomers"
    - from: "src/app/(dashboard)/crm/_components/customer-form.tsx"
      to: "src/lib/actions/crm.ts"
      via: "createCustomerAction / updateCustomerAction via useFormState"
      pattern: "createCustomerAction|updateCustomerAction"
    - from: "src/app/(dashboard)/crm/pipeline/_components/pipeline-board.tsx"
      to: "src/lib/actions/crm.ts"
      via: "updateDealAction for stage change"
      pattern: "updateDealAction"
---

<objective>
Build the CRM UI: customer list/create/edit/detail, leads management, pipeline board, and brand protection watchlist. These are the four admin-facing CRM surfaces covering CRM-01 through CRM-04 and CRM-06.

Purpose: Delivers the visible CRM functionality. Depends on 07-01 DAL. B2B portal (CRM-05) goes in plan 07-03 alongside windykacja.
Output: 12 new files across 4 CRM sub-sections; all routes build cleanly.
</objective>

<execution_context>
@C:/Users/mgwaj/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mgwaj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-crm-and-accounts-receivable/07-RESEARCH.md
@.planning/phases/07-crm-and-accounts-receivable/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Customer pages (list, create, edit, detail) + customer form and table components</name>
  <files>
    src/app/(dashboard)/crm/page.tsx
    src/app/(dashboard)/crm/new/page.tsx
    src/app/(dashboard)/crm/[id]/page.tsx
    src/app/(dashboard)/crm/_components/customer-form.tsx
    src/app/(dashboard)/crm/_components/customer-table.tsx
  </files>
  <action>
All page files are Server Components. Client components have "use client" directive.

**src/app/(dashboard)/crm/page.tsx** — Server Component. requireAdmin(). Accept searchParams: { search?: string; active?: string }. Call getCustomers({ search, isActive: active !== "false" }). Render PageHeader (title "Klienci CRM", "Nowy klient" GlowButton linking to /crm/new). Render CustomerTable (passing customers array and total count). Wrap in GlassCard with explicit padding (px-6 py-6 — Phase 02-03 GlassCard pattern).

**src/app/(dashboard)/crm/_components/customer-table.tsx** — "use client". TanStack Table pattern (Phase 02-02 URL state). Columns: Name (link to /crm/[id]), email, phone, nip, active badge (green/gray), lastContactAt (date-fns format "dd.MM.yyyy" or "—"), deals count, leads count. Search input pushes ?search= to URL via useRouter. Active toggle (all / active only) pushes ?active= to URL. Row action: deactivate button calls deleteCustomerAction(id) with confirm dialog. Use ColumnDef<CustomerRow>[] type.

**src/app/(dashboard)/crm/new/page.tsx** — Server Component. requireAdmin(). Get priceLists via getPriceLists() from DAL. Render PageHeader "Nowy klient". Render CustomerForm with mode="create" and priceLists prop.

**src/app/(dashboard)/crm/[id]/page.tsx** — Server Component. requireAdmin(). params.id → getCustomerById(Number(id)). If null, notFound(). Render: PageHeader with customer name + "Edytuj" button. Three GlassCards: (1) Customer details (contact info, NIP, account manager, last contact). (2) Leads table — simple HTML table of leads with status badge (new=blue, contacted=yellow, qualified=green, converted=purple, lost=red). (3) Deals list — cards with stage badge and value. CustomerForm below with mode="edit" prefilled.

**src/app/(dashboard)/crm/_components/customer-form.tsx** — "use client". react-hook-form with zodResolver. CRITICAL: useForm<z.input<CustomerSchema>> (not z.infer — Phase 02-02 decision). Fields: name (required), symbol, email, phone, nip, address (textarea), accountManager, priceListId (select from priceLists prop), isActive (checkbox). On submit: useFormState with createCustomerAction or updateCustomerAction. Show error/success from ActionState. After success redirect to /crm. GlowButton type="submit". GlassCard wrapper with px-6 py-6.
  </action>
  <verify>
    <automated>cd C:/xampp/htdocs/kalkulator2026 && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that /crm, /crm/new, /crm/[id] pages exist and import from correct DAL paths</manual>
  </verify>
  <done>npx tsc --noEmit exits 0. 5 files created. Customer table renders with search/filter URL state. Customer form uses z.input&lt;CustomerSchema&gt; with useFormState.</done>
</task>

<task type="auto">
  <name>Task 2: Leads list + Pipeline board + Brand protection watchlist</name>
  <files>
    src/app/(dashboard)/crm/leads/page.tsx
    src/app/(dashboard)/crm/leads/_components/lead-form.tsx
    src/app/(dashboard)/crm/leads/_components/lead-table.tsx
    src/app/(dashboard)/crm/pipeline/page.tsx
    src/app/(dashboard)/crm/pipeline/_components/pipeline-board.tsx
    src/app/(dashboard)/crm/brand-protection/page.tsx
    src/app/(dashboard)/crm/brand-protection/_components/brand-watch-table.tsx
  </files>
  <action>
**src/app/(dashboard)/crm/leads/page.tsx** — Server Component. requireAdmin(). searchParams: { status?: string; search?: string }. Call getLeads({ status, search }). PageHeader "Leady" + "Nowy lead" GlowButton that opens an inline create form (use local state-toggled modal approach — Phase 03-02 pattern). Render LeadTable. LeadForm in a dialog/modal state (show/hide with useState). Pass leads to LeadTable.

**src/app/(dashboard)/crm/leads/_components/lead-table.tsx** — "use client". TanStack Table. Columns: name, company, email, status badge (new=sky, contacted=amber, qualified=emerald, converted=violet, lost=zinc), source, createdAt. Status filter select pushes ?status= to URL. Edit button opens LeadForm in modal mode. Delete button calls deleteLeadAction(id).

**src/app/(dashboard)/crm/leads/_components/lead-form.tsx** — "use client". react-hook-form + zodResolver. useForm<z.input<LeadSchema>>. Fields: name (required), company, email, phone, source (text input), status (select: new/contacted/qualified/converted/lost). useFormState with createLeadAction or updateLeadAction. GlassCard with px-6 py-6.

**src/app/(dashboard)/crm/pipeline/page.tsx** — Server Component. requireAdmin(). Call getDeals(). Group deals by stage in server: create a Map<string, DealRow[]> for stages ["prospecting","proposal","negotiation","closed_won","closed_lost"]. Pass stageName→deals map as JSON-serializable object to PipelineBoard. PageHeader "Pipeline sprzedażowy". Compute totalValue per stage as sum of deal.value. Render PipelineBoard.

**src/app/(dashboard)/crm/pipeline/_components/pipeline-board.tsx** — "use client". Receives stages: Record<string, DealRow[]> + stageTotals: Record<string, number>. Renders a horizontal scrolling flex layout with one column per stage. Stage column has: header (stage label in Polish: prospecting="Prospecting", proposal="Oferta", negotiation="Negocjacje", closed_won="Zamknięte (wygrane)", closed_lost="Zamknięte (przegrane)"), total value display, list of deal cards. Deal card: title, customer name, value formatted as "X XXX,XX PLN". Each deal card has a stage selector (select dropdown) — on change calls updateDealAction(deal.id, { stage: newStage }) as a Server Action call via startTransition. After update, router.refresh(). No drag-and-drop needed — dropdown is sufficient per RESEARCH.md anti-patterns.

**src/app/(dashboard)/crm/brand-protection/page.tsx** — Server Component. requireAdmin(). Call getBrandWatchItems(). PageHeader "Ochrona marki" + "Dodaj pozycję" GlowButton (state-toggled modal). Render BrandWatchTable with items. BrandWatchForm inline in modal (use local state). Fields: url (text input), marketplace (text input — e.g. "allegro", "amazon"), productSku, notes, status (select).

**src/app/(dashboard)/crm/brand-protection/_components/brand-watch-table.tsx** — "use client". Simple HTML table (not TanStack — brand items are few and don't need sort/pagination). Columns: URL (truncated, external link), marketplace badge, productSku, status badge (active=green, flagged=red, resolved=gray), lastChecked (date or "Nie sprawdzono"), notes, actions (edit/delete). Delete calls deleteBrandWatchItemAction(id). Status update calls updateBrandWatchItemAction(id, { status }).
  </action>
  <verify>
    <automated>cd C:/xampp/htdocs/kalkulator2026 && npx next build 2>&1 | grep -E "(error|Error|Route|✓)" | head -30</automated>
    <manual>Confirm /crm/leads, /crm/pipeline, /crm/brand-protection routes appear in build output</manual>
  </verify>
  <done>npx next build exits 0. Routes /crm, /crm/new, /crm/[id], /crm/leads, /crm/pipeline, /crm/brand-protection all listed in build output. Pipeline board groups deals by 5 stages with total values.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` exits 0
2. `npx next build` exits 0
3. Build output shows: /crm, /crm/new, /crm/[id], /crm/leads, /crm/pipeline, /crm/brand-protection
4. CustomerForm uses useForm&lt;z.input&lt;CustomerSchema&gt;&gt; (not z.infer)
5. GlassCard has explicit px-6 py-6 padding on all pages
6. Pipeline board renders 5 stage columns with deal cards
</verification>

<success_criteria>
- All 12 CRM UI files created and TypeScript-clean
- Customer list supports search and active/inactive filter via URL state
- Customer soft delete (isActive=false) called from table row action
- Leads list has status filter; lead create/edit via state-toggled modal
- Pipeline board shows all 5 stages; stage change via dropdown + server action
- Brand watchlist supports full CRUD via simple table
</success_criteria>

<output>
After completion, create `.planning/phases/07-crm-and-accounts-receivable/07-02-SUMMARY.md`
</output>
